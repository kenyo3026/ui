WS_PORT=7415

# Provider: openai | anthropic | google | openai-compatible
PROVIDER=openai
MODEL=gpt-4o
API_KEY=

# Only required when PROVIDER=openai-compatible
BASE_URL=

WORKSPACE=/path/to/workspace
MCP_CONFIG_PATH=./mcps.json
